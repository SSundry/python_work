#The simplest if statement has one test and one action:

#if conditional test:
	#do something

#If the statement is Ture Python executes it and if it's False Python ignores it
age= 17
if age>= 18:
	print("You are old enough to vote!\nHave you registered to vote yet?")

#if-else statements allows the else part to define an action(s) that are exected when the conditional test fails
else:
	print("Sorry you are too young to vote.")
	print("Please register to vote as soon as you turn 18!")